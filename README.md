# CODL

Public static deployment repository.

- The private source workspace is maintained separately as `CODL-source`.
- The public deployment repository is `CODL`.
- Local filesystem paths and private operational details must not be committed here.
